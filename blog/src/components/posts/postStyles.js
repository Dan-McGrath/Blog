import styled from "styled-components";

const Wrapper = styled.div``;

const Title = styled.h3``;

const Article = styled.article``;

const User = styled.p``;

export { Wrapper, Title, Article, User };
