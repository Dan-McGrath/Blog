import styled from "styled-components";

const Wrapper = styled.div``;

const Title = styled.h3``;

const ErrorWrapper = styled.div``;

const ErrorText = styled.p``;

export { Wrapper, Title, ErrorText, ErrorWrapper };
